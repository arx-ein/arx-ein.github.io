---
import Header from '@/components/Header.astro'
import Sidebar from '@/components/Sidebar.astro'
import Footer from '@/components/Footer.astro'
import '@/styles/default.css'
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/mycelithyl-hyphae/hyphae-logo.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title} | mycelithyl-hyphae</title>
    </head>
    <body class="h-svh dark:bg-zinc-900">
        <Header></Header>
        <div class="md:flex">
            <Sidebar></Sidebar>
            <main class="flex flex-1 justify-center">
                <div class="w-full max-w-screen-md px-8">
                    <h1 class="text-center text-green-700 dark:text-lime-400 text-4xl my-6">
                        {title}
                    </h1>
                    <slot />
                </div>
            </main>
        </div>
        <Footer></Footer>
    </body>
</html>
